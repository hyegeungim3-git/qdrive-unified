# Qdrive 대통합 — 프로젝트 컨텍스트

대구 시내버스 통합 플랫폼의 사업 제안용 라이브 데모. **두 선행 프로토타입을 하나의 통일된 플랫폼으로 통합한 정본**이다.

## 출신 (통합 대상)
- **base = qdrive-proto** (Vite/React 19/TS/Tailwind 4): 실제 시뮬레이터 엔진 + 이해관계자 탭. 이 저장소의 뼈대.
  - 원본: `../Qdrive/qdrive-proto`, 구 저장소 github.com/hyegeungim3-git/qdrive-proto
  - **proto 업그레이드 동기화 지점: `64cfa39`** (2026-07-13 반영). 이후 원본이 더 나가면 `git -C ../Qdrive/qdrive-proto log --oneline 64cfa39..HEAD`로 델타 확인 후 반영.
- **탄소 플랫폼** (정적 HTML `.dc.html` + dc-runtime, 토스 디자인): 탄소중립 서사·V2G/AI Planning 시뮬레이터·시민 공개 대시보드·운수사 경영 손익. 기능/콘텐츠를 React로 이식.
  - 원본: `../AI 기반 탄소중립 운영 플랫폼`, 구 저장소 github.com/hyegeungim3-git/qdrive-carbon-platform
- 이 통합본 저장소: **로컬 git만** (main 브랜치). GitHub 원격은 미연결 (사용자 지시 시 연결).

## 핵심 통찰 (전 화면 기능 분석 결과)
두 데모는 같은 도메인을 **다른 렌즈**로 봄 → 대부분 상충이 아니라 **상보**.
- **proto = 살아있는 운영 엔진**: 기능 대부분이 시뮬레이터 연동 **실동작**(민원→증빙 자동매칭, Agentic 승인 루프, 정당판정+음성소명 왕복, 파생 센서, 자동생성 리포트). 엔지니어링 깊이 압도적.
- **탄소 = 탄소중립 서사 + What-if**: 대부분 정적 목업이나 proto에 없는 것 보유(탄소·연료 인과사슬 r=0.81, 경영 손익 P&L, 시민 공개 페이지=계산기·공유는 실동작, AI Planning·V2G 시뮬레이터, 라이브 AI 코파일럿=실제 Claude).
- **통합 원칙**: **살아있는 코어는 proto, 사업가치 레이어는 탄소.**
- 참조: 통합 설계도 아티팩트 https://claude.ai/code/artifact/95f41e46-d497-4c44-a229-33512f820722

## 현재 확정 IA (2026-07-13 서비스 구조 재편 후 — 최신)
최상위 탭 7개, **"누가 보는가" 단일 축**: 시티 · 운수사(8서브탭: 관제/💰경영·투자/운행이력/AI리포트/연료·에코AI/진단스캐너/정비도우미/차고지) · 기사(+배지·인사이트+내 에이전트) · 승객 · **🌱 탄소중립 분석**(성과 증명 전용, 2서브탭: 탄소·연료/안전운행) · 실증리포트 · 로드맵
별도 진입점: **시민 공개 페이지**(`#citizen`) · 공통 오버레이: **AI 코파일럿**(엔진 규칙조회+라이브 Claude, 전 탭)
AI 업무센터·에이전트 플랫폼은 최상위 탭에서 해체돼 각 소속 탭의 조치함/내 에이전트/승인함으로 흡수됨 — 상세는 하단 "서비스 구조 재편" 절 참조. 이 절 아래 매트릭스·탭 목록은 그 재편 **이전** 스냅샷이므로 탭 개수 등 일부 옛 정보 포함(이력 보존용).

## 배포 (2026-08-19 Cloudflare Pages 전환 — 최신)
- **라이브(정본)**: https://qdrive-unified.pages.dev/ — Cloudflare Pages, 계정 hyegeungim3@gmail.com
- **시민 공개**: https://qdrive-unified.pages.dev/#citizen
- **재배포(수동)**: `npm run build` 후 `npx wrangler pages deploy dist --project-name=qdrive-unified --branch=main --commit-dirty=true`
- **재배포(자동)**: `.github/workflows/cloudflare-deploy.yml` — main push 시 Actions가 빌드→업로드. **GitHub Secret `CLOUDFLARE_API_TOKEN`(권한 Account/Cloudflare Pages/Edit) 등록이 전제**이며, 미등록이면 이 워크플로만 실패하고 수동 배포는 계속 가능.
- **저장소 공개 여부와 배포는 무관**: Pages 프로젝트가 Direct Upload 모드(`Git Provider: No`)라 Cloudflare가 저장소를 조회하지 않는다. 저장소를 비공개로 바꿔도 pages.dev는 그대로 동작하고, 자동배포 워크플로도 비공개 저장소에서 동작한다(Actions가 저장소 내부에서 빌드·업로드). 단 **GitHub Pages는 비공개 전환 시 내려간다**(Pro/Team 이상만 서빙).
- **저장소(공개)**: https://github.com/hyegeungim3-git/qdrive-unified
- **GitHub Pages(보조·유지)**: https://hyegeungim3-git.github.io/qdrive-unified/ — main push 시 Actions 자동 배포. 기존 공유 링크 보호용으로 살려 둠.
- **base 경로 이원화**: vite base 기본값은 `'/'`(Cloudflare Pages는 루트 서빙). GitHub Pages는 저장소 하위경로라 `.github/workflows/deploy.yml`이 `BASE_PATH=/qdrive-unified/`를 넘겨 오버라이드. **vite.config.ts의 base를 직접 하드코딩하지 말 것** — 두 배포처가 갈라진다.
- `public/_redirects`(`/* /index.html 200`) — Cloudflare Pages SPA 폴백. 해시 라우팅(#citizen)이라 필수는 아니나 임의 경로 404 방지.
- **BIS 프록시 Worker**(`worker/`, qdrive-bis-proxy.hyegeungim3.workers.dev): `ALLOWED_ORIGINS`에 `https://qdrive-unified.pages.dev` 추가·재배포 완료. **새 도메인을 붙이면 이 목록도 같이 고치고 `npx wrangler deploy` 할 것** — 안 하면 실차 오버레이만 조용히 죽는다.
- **Windows Git Bash 함정**: `BASE_PATH=/qdrive-unified/ ...`를 Git Bash에서 실행하면 MSYS 경로 변환이 값을 `C:/Program Files/Git/qdrive-unified/`로 바꿔 버린다. 로컬 확인 시 `MSYS_NO_PATHCONV=1` 필수(GitHub Actions는 Linux라 무관).

## 통합 매트릭스 (best-of-both — 진행 상태)
| 통합 대상 | proto 기반(유지) | 탄소 플랫폼에서 이식 | 결정 | 상태 |
|---|---|---|---|---|
| 시티 대시보드 | CityDashboard + PolicyReport | — (탄소 분석은 별도 탭으로 분리) | 유지 | ✅ 유지 |
| 🌱 탄소중립 분석(신규 탭) | (엔진 kpi 집계) | 탄소·연료·안전 대시보드(r=0.81), AI Planning·V2G 시뮬레이터 | **엔진 일부 연결→실동작 승격** | ✅ 완료(carbon 탭, 3서브탭) |
| 운수사 관제 | OperatorView 6서브탭 | 경영 손익(P&L)·V2G 잠재수익 → "경영 요약" 서브탭 추가 | ADD 서브탭 | ✅ 완료(💰 경영 요약 서브탭) |
| 기사 앱 | DriverApp(랭킹·코칭·소명 유지) | 배지 6종·퍼스널 인사이트만 (탄소 정적 랭킹은 DROP) | MERGE 일부 | ✅ 완료(프레임 하단 리포트 섹션, MVP 배지 rank 연동) |
| 승객 앱 | PassengerApp | — | 유지 | ✅ 유지 |
| 시민 공개(별도 진입) | (kpi 집계 연결) | 시민 탄소 페이지(히어로·소나무·계산기·공유·등급) | ADD 진입 | ✅ 완료(#citizen, 39721e3) |
| 실증리포트/로드맵 | ReportView / TeaserView | — | 유지 | ✅ 유지 |
| 공통 | 다크모드·DemoControls·BIS worker | 라이브 AI 코파일럿, 토스급 상태디자인 완성도 | ADD·통일 | ✅ 완료(코파일럿·라이트/다크·375 반응형 검증) |

**디자인 통일 원칙**: proto의 Paperlogy + Tailwind가 정본. 탄소의 토스 토큰/Pretendard는 가져오지 않고 완성도(상태 디자인·마이크로인터랙션·서사)만 흡수. 임의 색·임의 px 금지, 토큰만 사용.

## 이식 시 상충 해소 (확정)
- 기사앱 **랭킹 중복** → proto(엔진 실시간) 채택, 탄소 정적 랭킹 DROP. 배지·인사이트만 이식.
- **디자인 이원화** → Paperlogy+Tailwind 정본 통일.
- 코파일럿: proto worker는 BIS 전용(AI 아님)이라 순수 추가.

## 아키텍처 (핵심만 — base 상속)
- `src/sim/engine.ts` — 시뮬레이터 심장. 250ms 실틱 × 배속, 1초 서브스텝. 버스 9대(3노선),
  기사 페르소나 A모범/B평균/C개선필요, 연비→CO₂ 모델, 데모 트리거(triggerRiskEvent/
  triggerFault/fileComplaint/forceRecommendation/cycleWeather), 하차예약(reservation)
- `src/sim/store.ts` — 엔진 싱글턴 + useSyncExternalStore. DEV에서 window.__engine 노출
- `src/sim/types.ts` — **실데이터 교체 지점**: PacketSource 인터페이스. 실단말 연동 시 SimEngine 대신 RealPacketSource 구현으로 스왑
- `src/sim/bis.ts` — 대구 BIS 실차 오버레이 (TAGO 오픈API, vite 프록시 /tago, 로컬 dev만. 키는 localStorage 'qdrive-bis-key', 절대 커밋 금지)
- `src/sim/routes.ts` — 노선 폴리라인(주요 간선 근사)
- `src/views/` — 탭당 1파일. OperatorView는 서브탭(관제/진단스캐너/AI정비챗/차고지)
- 테마: `src/theme.ts` + index.css의 html.light 변수 반전
- 폰트: Paperlogy 7웨이트, public/fonts, url('../fonts/...') 상대경로

## 도메인 요점
- 주인공 차량 = 대구70자3742 (김성호 기사, 급행1) — 모든 데모 시나리오의 중심
- 반월당 = 3개 노선이 모두 지나는 기준 정류장 (승객 앱 ETA·탑승 기준점)
- 위험운전 8종(공단 기준), eTAS 자동제출, 준공영제 정산 검증(BMS×DTG 교차)
- 탄소 정합 수치(탄소 플랫폼 기획서 §4 정합 사전 상속): 경유 배출계수 2.68 kgCO₂/L, 소나무 6.6 kgCO₂/그루·년. 이식 시 수치 정합 먼저 확인.

## 결정사항 (뒤집으려면 사용자 확인)
- 백엔드 없음 (데모는 오프라인 생존 우선) / 커밋 메시지 한국어, PowerShell here-string·쌍따옴표 금지
- 통합 방식: React 단일 앱 (dc-runtime 정적 HTML은 base로 채택하지 않음 — 확장성)
- git identity는 저장소 로컬 (hyegeungim3-git), --global 금지

## 검증
- `npm run build` 통과 확인. 폰트 `../fonts/...` 미해결 경고는 런타임 해석이라 정상.
- **vite 8(rolldown) 빌드 크래시 함정**: `node_modules/.vite` 캐시가 손상되면 "644 modules transformed" 직후 청크 렌더링 단계에서 네이티브 크래시(exit -1073740791, STATUS_STACK_BUFFER_OVERRUN, 패닉 메시지 없음). **해결: `node_modules/.vite`와 `dist` 삭제 후 재빌드.** 코드 문제로 오인 금지.
- PowerShell에서 `npm run build 2>&1`은 vite stderr를 NativeCommandError로 감싸 exit 9로 보임 → 실제 판정은 `cmd /c "npx vite build > log 2>&1"` 후 `$LASTEXITCODE`로.
- 브라우저 검증은 DOM 텍스트/상태 우선 (스크린샷은 라이브 카운터 리렌더로 타임아웃 잦음). 콘솔의 Electron sandboxed_renderer 에러는 프리뷰 하네스 내부 문제로 앱과 무관.

## 빌드 시퀀스 (각 슬라이스: 이식→빌드→브라우저 검증→커밋)
1. **시민 공개 페이지**(별도 진입) ← 자기완결·실동작 위젯 많음, 첫 검증 슬라이스
2. **🌱 탄소중립 분석 탭**(신규 최상위) ← 탄소·연료·안전 대시보드 + AI Planning·V2G(엔진 kpi 연결)
3. **운수사 경영 요약 서브탭** ← 손익 P&L(엔진 연료절감·CO₂ 연결)
4. **기사앱 배지·인사이트** ← DriverApp 리포트 섹션 보강
5. **라이브 AI 코파일럿**(공통 오버레이) ← 사용자 키→Claude 직접호출
6. ✅ 디자인 통일·모바일 반응형·README 통합본 정리 — 완료(base `/qdrive-unified/`, 라이트/다크·375 검증)

## 대통합 1차 완료 (2026-07-13)
6개 슬라이스 전부 이식·검증·커밋. 라이트(기본)/다크 테마·375px 반응형 무오버플로 확인. 원본 2개 프로젝트 무손상.

## proto 업그레이드 반영 (2026-07-13, 51914ca→64cfa39)
원본 proto 8커밋 델타를 통합본에 반영. 별도 저장소라 cherry-pick 대신 **무충돌 파일 통째 복사 + 충돌 파일 proto판 복사 후 내 추가분 재적용** 방식.
- **proto 신규(통째 반영)**: engine.ts(headway·ecoScore·fuelWaste·예측형 에코코칭)·types.ts·agentRequests.ts / 뷰: AgentCenter(AI 업무센터 탭)·AgentPlatform(에이전트 플랫폼 탭)·operator/EcoFuel(연료·에코 AI 서브탭) / PolicyReport·AiReport 소폭 / DEMO_GUIDE.md
- **충돌 파일 병합**: App(proto agent/platform 탭 + 내 carbon/citizen), OperatorView(proto eco 서브탭·headway 열·소명순화 + 내 biz 서브탭), DriverApp(proto headway·에코·소명순화 + 내 리포트), Copilot(**proto 엔진 규칙기반 코파일럿 + 내 라이브-Claude 모드 병합** — 추천질문=규칙조회, 자유입력=라이브키 시 실제 Claude)
- 최종 탭 9개: 시티·운수사·기사·승객·🌱탄소중립분석·AI업무센터·에이전트플랫폼·리포트·로드맵 (+시민공개 #citizen, +코파일럿 오버레이). 빌드 통과(JS 1029KB), 전 기능 DOM 검증.

## 서비스 구조 재편 (2026-07-13, proto 업그레이드 반영 직후)
사용자 요청: "겹치는 것 제외하고는 각 원본 기능이 잘 어우러져 표현되면 좋겠다" — 9탭 진단 아티팩트(https://claude.ai/code/artifact/6559ac4f-dbeb-48a0-b9b4-f1a56b526e9e)의 실행 옵션 C를 이 원칙으로 수행. 겹치는 부분만 통합하고 고유 기능은 보존·재배치.

**슬라이스1 — AI 어시스턴트 통합**: 에이전트 플랫폼 탭 해체. 회사 롤 규칙조회(코파일럿과 중복)는 제거. 고유 기능인 기사 개인 Q&A+빠른신청은 `DriverApp.tsx`의 `MyAgent` 컴포넌트로, 회사 승인함은 `OperatorView.tsx`의 "기사 요청 승인함" 패널로 이관. 공유 상태는 기존 `sim/agentRequests.ts` 그대로 재사용(기사↔회사 왕복 검증 완료). `AgentPlatform.tsx` 삭제.

**슬라이스2 — AI 업무센터 재배치**: TASKS·카드로직을 `components/ActionCenter.tsx`로 추출(owner 필터, 토글 없음). 대구시 3업무 → `views/city/ActionCenterModal.tsx`(PolicyReport와 동일 모달 셸). 버스회사 3업무 → OperatorView 관제현황 인라인 패널(접기/펼치기, 승인건수 배지). `AgentCenter.tsx` 삭제. owner 필터링 검증 완료(시티엔 대구시 업무만, 운수사엔 버스회사 업무만).

**슬라이스3 — 경영·투자 통합**: 탄소중립 분석의 AI Planning(전기전환·V2G 시뮬레이터 + 전환대상TOP5)을 `operator/BizSummary.tsx`로 이관, 서브탭명 "💰 경영 요약"→"💰 경영·투자". 탄소중립 분석은 2서브탭(탄소·연료/안전운행)으로 슬림화 — 순수 성과 증명 전용. 탭 간 서브탭 딥링크는 신규 `sim/navIntent.ts`(1회성 인텐트, OperatorView가 마운트 시 소비)로 구현 — 탄소중립분석의 크로스링크 배너 클릭 시 운수사 "경영·투자" 서브탭으로 직행 검증 완료.

**결과**: 탭 9개 → 7개. 각 해체된 탭의 고유 기능은 소실 없이 더 적합한 맥락으로 재배치(위 "현재 확정 IA" 절 참조). 전 슬라이스 빌드+새 브라우저탭 콘솔 0에러+기능 왕복 검증. 원본 두 프로젝트 무손상.

## 기사앱 리포트 화면 보완 (2026-07-13, 슬라이스4 재검토)
사용자 지적으로 재검토 — 슬라이스4("기사앱 배지·인사이트")에서 원본 탄소 플랫폼 기사앱 리포트 탭의 **절반을 누락**했던 것을 발견·수정. 원인은 의도적 제외가 아니라 초기 스코프를 "배지·인사이트"로 좁게 잡으며 나머지를 검토 목록에서 빠뜨린 실수. 특히 "사내 랭킹"은 "proto 실시간 순위와 중복"이라 판단해 DROP으로 기록했으나, 재검토 결과 원본은 동료 목록이 보이는 소셜 비교 요소이고 proto 홈 위젯은 숫자 하나(N위/9명)뿐이라 실은 겹치지 않는 콘텐츠였음 — 판단 실수.

추가된 것(리포트 탭, `ReportScreen`): 이번 주 KPI 3종(서사값+오늘 실측 각주) · 주간 연비 추이 차트(내 연비 vs 회사평균, 원본 데이터 그대로) · **내 운행 기록(엔진 실시간 trips 필터링 — 원본은 정적 4건, 지금은 실동작으로 승격)** · **사내 안전운전 랭킹(엔진 9대 전원 실시간 — 원본은 정적 5명, 지금은 전원 실동작)** · 개선 포인트 AI 코칭 팁 3종. 기존 배지·인사이트·에이전트와 함께 원본 리포트 탭 전 구성 완성.
검증: 8개 섹션 렌더 확인, 랭킹 실시간 변동 확인, 배속 상승 시 운행기록 실제 축적 확인(2회차→1회차 최신순, 노선·거리·연비·CO₂ 실데이터).

남은 후보: AI Planning·V2G의 엔진 심화 연결 / 스크린샷 기반 화면설계서 / EcoFuel↔탄소중립분석 교차링크(경미, 미착수).

## 전체 재검수 + 누락 콘텐츠 복원 (2026-07-14)
사용자 요청: "각 폴더에서 뺐던 내용/기능/컨텐츠가 있으면 뺀 이유를 말하고, 아니면 전체 재검수하고 어울리게 만들어줘. UI/UX도 검수하고 사용성·직관성을 높여줘. 꼼꼼히."

**멀티에이전트 감사(Workflow, 104/105 에이전트)** 로 두 원본 vs 통합본 전수 대조. 결과:
- **UI/UX 결함 37건** → 발주처 신뢰·정확성(배치1), 발주처·민감독자 톤(배치2), 라이트 모드 접근성·대비 AA(배치3)로 3배치 커밋 완료(77c0a43·e18da41·5850c45). 배치1에서 경쟁사명·시연지시문·과장 배지 제거, 클립보드 폴백, 페르소나 톤(개선필요→코칭 대상) 등. 배치3에서 라이트 팔레트 캡션 대비 AA 확보 + 차트 축/격자 CSS 변수화(recharts에서 var() 실렌더 확인).
- **누락 콘텐츠 19건** 식별(10 restore-live / 5 restore / 4 drop-justified). 사용자 확정 범위(정책제안+KOC)만 우선 복원(8ca99cf):
  - **PolicyReport**: "🏙️ 데이터 기반 정책 제안" 3건(반월당 신호주기→교통정책과 / 만평네거리 정류장 이설→도로관리과 / 신천대로 전용차로 연장→대중교통과). 개인 코칭을 시설·환경 개선으로 보완하는 도시 레벨 서사. 부서 연계 명시.
  - **CarbonAnalysis(탄소·연료)**: "♻️ KOC 크레딧 실적명세". 확정분(321.1 tCO₂/2,858,000원/OBD×DTG/8월 제출) + kpi.totalCo2SavedKg 실시간 크레딧 환산. "실측이 자산이 되는" 구조 라이브 시연.
- **관제·시티 첫인상 배치(A그룹 6건) 복원 완료 (2026-07-14)**: 사용자가 전체 목록 확인 후 이 배치를 선택. 감사 요약대로 저비용·첫인상 회복 효과 최상 항목들.
  - **성과 리본**(CityDashboard 최상단 전폭): 탄소·연료 라이브 kpi(연료절감·CO₂·안전점수·주행) + "🌱 탄소중립 분석에서 성과 증명 →" CTA(onNavigate('carbon')). CityDashboard에 onNavigate prop 신설(App.tsx 주입).
  - **AI 3카드**(우열 KPI 아래): AI 운행 인사이트/안전 코치/정비 예측 — snap 파생 라이브, 각 카드 딥링크(insight→carbon, coach→driver, forecast→operator 진단스캐너 via navIntent).
  - **AI 추천 4건 글랜스**(OperatorView 관제현황 상단): 전기전환→biz·충전→depot·공회전코칭→eco·정비진단→chat, 각 카드 setSub 딥링크. '지금 AI가 추천하는 4가지'를 내비 없이 한눈에.
  - **차량 이상 트리아지**(CityDashboard 좌열): 개별차량×OBD/DTG×심각도배지(위험/주의/정보)×경과시간 — snap.fault(위험)·최근 이벤트 120s(주의)·score<72(정보) 라이브 집계. 위젯 토글 'triage' 추가.
  - **계통별 가동률**(CityDashboard 좌열): 급행·간선·지선·순환 운행/보유 프로그레스 바 — 도시 전체 준공영제 스케일 정적(9대 라이브 스케일 오도 방지 명시). 위젯 토글 'network' 추가.
  - **지도 오버레이**(MapView): 줌 +/− 버튼(44px 터치, 좌하단 아닌 우하단)·LIVE 배지·펄스(좌상단)·히트맵 그라디언트 범례(ON 시). **줌 버그 수정**: ①L.DomEvent.disableClickPropagation은 React 19 루트위임 onClick을 삼켜 클릭 무효 → disableScrollPropagation만 사용. ②애니메이션 줌(zoomIn())은 엔진 250ms 리렌더가 leaflet 줌 애니메이션(~250ms)을 중간 취소 → setZoom(±1, {animate:false}) 즉시 줌으로 교체. 브라우저 검증: 13→15→14 실동작.
  - 검증: 타입체크·빌드 통과. 6항목 전부 DOM 렌더 + 상호작용(CTA·딥링크·줌) 실동작 확인. 앱 콘솔 에러 0(Electron sandbox·편집중 HMR 잔상 제외). 스크린샷은 leaflet+recharts+라이브카운터로 타임아웃 → DOM/기능 검증으로 판정.
- **차량 관리 배치(B그룹 4건) 복원 완료 (2026-07-14)**: 사용자 "B" 선택. Ultracode 다중에이전트 워크플로로 원본 dash V(탄소중립 대시보드.dc.html) 정확 추출 → 코드베이스 매핑 → 응집성/데이터모델 종합(9에이전트) 후 구현. **엔진/타입 확장 0**(최소확장 원칙 — 회사 스케일 수치는 9대 실증과 스케일이 달라 라이브 부적합, 라이브 필요값은 전부 기존 필드로 파생).
  - **신규 `src/views/operator/VehicleRegistry.tsx`** = 관제 서브탭 9번째 "🚌 차량 관리"(id 'vehicles', depot 다음). 자산·대장 렌즈로 4항목을 응집(운영 렌즈 ops와 분리):
    - **KPI 4카드**(item1): 등록412·정비입고12·예지정비5·평균차령5.8 정적 헤드라인 + 라이브 각주(실증 9대·발행 workOrder·초안 workOrder). 예지정비 딥링크→scanner. KpiCard 재사용.
    - **라이브 9대 브리지**: snap.vehicles 실시간 자산행(누적주행=odoBase+distanceKm, 상태배지=fault/dwell 파생, ops:431 미러). LIVE 칩.
    - **정적 자산 대장**(item4): 준공영제 5개사 예시 7행(REGISTRY 상수, 회사명=CarbonAnalysis CO_RANKS 정본 세운/세진/경북/신흥/동명, 유종·차령·누적주행·최근정비일). 검색 실동작(id/회사/유종 부분일치)+빈상태. '차량 등록'=disabled no-op.
    - **상태 도넛**: recharts PieChart 운행356/정비12/대기44=412 (정적).
    - **정비 이력**(item2): FIX_LOGS 3건(3742/5563/0917 — ops '정비비 예측'과 동일 ID로 예측→완료 짝) + 발행 workOrder 있으면 라이브 승격 1건 prepend(비용 '(예상)' 접미로 구분).
  - **관제현황 검색**(item3, OperatorView 3-패치): 로스터 테이블에 차량번호·기사·노선 부분일치 검색 + '{n}/9대' 카운터 + 빈상태(colSpan=11). 4항목 중 유일 무중복 신규분. co(운수회사) 필터는 VehicleState에 필드 없어 노선축으로 대체.
  - **스케일 3단 분리**(오도 방지): 시412 / 5개사 예시대장(회색 '준공영제 5개사·예시 대장' 칩) / 실증9(emerald '실증 9대·실시간' 칩). 412=5개사 합계(세운98 포함)·실증9 포함관계 헤더 명시. CityDashboard NETWORK_UTIL 선례 계승.
  - 검증: 타입체크·빌드 통과. DOM+기능 전수 — 서브탭 6섹션 렌더 / 자산검색 왕복('전기'→2행·경유숨김, 'zzz'→빈상태, 복귀) / 관제검색 왕복('급행'→6/9, 'zzz'→빈상태, 복귀→9/9) / 예지정비 딥링크→진단스캐너 / **라이브 승격 폴링**: triggerFault→초안 각주 0→1·발화배지, approveWorkOrder→발행 각주 0→1·정비이력 라이브카드 출현. 앱 콘솔 에러 0.
  - **어드버세리얼 리뷰 패스(2026-07-14, 17에이전트 3차원×회의적 재검증)로 확정 결함 8건 수정**(커밋 b6f4b02): ①**412 축소 오도** — '시 전체 412대'는 오류(대구 CNG 시내버스≈1,513대·26개사가 정본, 412는 준공영제 참여 5개사 합계). '준공영제 참여 5개사 412대'로 재라벨(헤더·도넛캡션·주석 3곳, CitizenPublic '참여 버스' 어조 통일). ②도넛 Tooltip 배경 var(--color-gray-900)→라이트 #fff 반전으로 밝은 세그먼트색 텍스트 대비 실패 → 테마 불변 다크 #191f28 고정. ③유종 'cng' 소문자 검색 0건 → toLowerCase 정규화. ④라이브 표 최근정비 시각 fault.startedAt→발행 workOrder.createdAt(정비이력 카드와 90초 불일치 제거). ⑤LIVE 표 하드코딩 '2026-06월'→'—'(발화 시 실시각). ⑥onSub 타입 string→'scanner' 좁힘. ⑦헤더 text-sm→text-lg(BizSummary 통일). ⑧**후속 검증에서 발견**: 도넛 Pie가 250ms 리렌더에 애니메이션 취소돼 아예 안 그려짐 → isAnimationActive={false}(라이브 차트 선례). **교훈: recharts를 useSim() 라이브 뷰에 넣을 땐 isAnimationActive={false} 필수(지도 줌 애니메이션 취소와 동일 계열 함정).**
- **운전자 관리 배치(C그룹 5건) 복원 완료 (2026-07-14)**: 사용자 "이어서" 지시. Ultracode 다중에이전트 스펙 워크플로(11에이전트) → 구현. 엔진/타입 확장 0.
  - **신규 `src/views/operator/DriverRegistry.tsx`** = 관제 10번째 서브탭 "👥 기사 관리"(id 'drivers', vehicles 다음). 차량=자산 렌즈와 대칭인 사람=인사·성과 렌즈. VehicleRegistry 구조 미러(useSim·KpiCard·Panel·StaticChip/LiveChip·onSub).
    - KPI 4카드: 등록기사 486(정적) / 평균운전점수(라이브 avgScore) / 교육대상 23(정적)+실증 score<78 각주 / 우수기사 64(정적)+실증 score>=90 각주.
    - 성과관리 Panel: 에코 달성률(라이브 ecoScore 9대 평균, emerald 진행바 — CSS div, recharts 아님) + 인센티브 지급 12,800,000원(정적) + 교육 이수율 91%(정적 sky 진행바) + 지표축 각주.
    - 실증 기사 성과 분포: 우수/일반/교육대상 라이브 count(실증 9명), 교육대상 카드 클릭→onSub('ops') 관제 조치함 딥링크.
  - **값 충돌 정합**(78/91 이원화 해소): 에코 달성률=라이브 ecoScore 평균(시드 77.8≈78 자연정합). 78%=CitizenPublic:216 '에코 드라이빙 실천율 (5개사 평균)'로 스코프 명시. 91%=BizSummary:200 '세운버스 에코 실천율 91% (준공영제 5개사 평균 78%)'로 세운 스코프 명시. 교육 이수율 91(수료)은 다른 서브탭·스케일·지표축(수료 vs 참여)+각주로 에코 실천율 91과 디커플.
  - **486 스케일 라벨**: '준공영제 참여 5개사 486명 · 412대 (세운 98 포함) · 실증 9명 라이브'. 412 재라벨(b6f4b02) 선례 계승, '시 전체' 금지. 486(기사)>412(차량)=교대·예비.
  - OperatorView 3-패치 + **subNav flex-wrap**(10번째 버튼 오버플로 방지).
  - 검증: 빌드 통과. DOM+기능 — 4KPI·성과관리 3행·분포 렌더 / 에코 진행바 라이브 동기(79%→80% 활동 시 변동, width=값) / 성과분포 3/4/2 합9 / 교육대상 딥링크→관제현황 / BizSummary·CitizenPublic 정합 편집 확인. 앱 콘솔 에러 0.
- **전체 감사 restore 후보 19건 전량 처리 완료** (A그룹 6 + B그룹 4 + C그룹 5 + 앞서 정책제안·KOC 2 + drop-justified 4). 남은 후보 없음.

## UI 최적화 패스 (2026-07-14, 사용자 지적 → 전체 훑기)
사용자가 스크린샷으로 UI 깨짐 지적 + "전체적으로 훑어보고 최적화".
- **사용자 직접 지적 3계열**(커밋 4b5a2d5): ①BizSummary '우리' 배지 고정폭 w-24 줄바꿈→w-32+nowrap. ②저대비 텍스트 — index.css html.light에 emerald/red/violet-200 오버라이드 **누락**(sky/amber-200만 있었음) → #065f46/#991b1b/#5b21b6 추가(체계적). ③**도넛 공백** — recharts Pie가 엔진 250ms 리렌더에 애니메이션 취소돼 sector path 미렌더. BizSummary·CarbonAnalysis 두 도넛 isAnimationActive={false}(VehicleRegistry 도넛·지도 줌과 동일 계열). **교훈 재확인: 라이브(useSim) 뷰의 모든 recharts에 isAnimationActive={false} 필수.**
- **전 뷰 반응형 감사**(다중에이전트 69, 62 스캔 → 확정 24, 커밋 f9c2499): 전부 협폭(375/768) 가로 오버플로. 대부분 데스크톱 무영향 브레이크포인트 추가. 핵심: App 헤더탭 flex-wrap / CityDashboard 3분할 grid lg: 분기+지도 max-lg:min-h / OperatorView 로스터 11열 overflow-x-auto 래퍼 / 각 grid-cols-N에 max-[900px]·max-[860px] 분기 / PassengerApp flex-col xl:flex-row / 모달 헤더 min-w-0+닫기 shrink-0 / Copilot max-h-[calc(100dvh-6rem)] / **DriverApp 리포트: 고정 1020px 태블릿 프레임(scale 축소) 내부의 뷰포트 브레이크포인트 5곳 제거**(오작동). 검증: 375px 최상위 7탭·운수사 8서브탭·시민 전부 body 오버플로 **0**(이전 260px+), 768 전 탭 0, 데스크톱 1280 3분할 그리드 유지 회귀 없음.
- **잔여 오탐 기록**: 감사 [22] DriverApp 운전석 flex는 프레임이 scale로 축소되므로 '모바일 눌림' 전제가 틀림 → 스킵. DriverApp:718 max-[860px]은 리포트 밖 다른 스크린이라 보수적 유지.

## 성과 검증 탭 신규 + 폰트 수정 (2026-07-14)
사용자 요청: 전략 문서(데이터·AI·온톨로지→서비스화→성과증명)의 결론부를 실동작 화면으로.
- **폰트 깨짐 수정**: TeaserView(로드맵) 데이터 원천 라벨이 `font-mono`였는데, 영문약어(DTG)+한글(차고지/충전소·정비시스템)이 섞여 한글이 mono 글리프 부재로 폴백돼 깨져 보임 → `font-mono` 제거, Paperlogy 브랜드 폰트로 통일. **교훈: 한글 포함 텍스트에 font-mono 금지**(차량ID 등 숫자 위주 표 정렬용만 유지).
- **신규 `src/views/PerformanceProof.tsx`** = 최상위 탭 "🔬 성과 검증"(carbon 다음). 전략 §6(유의미한 결과 4단 증명)의 실동작판. 5섹션: ①성과귀속(반사실 비교) ②A/B(페르소나 그룹) ③기준선 ④서비스별 신뢰지표 테이블 ⑤4단 게이트.
  - **핵심 = 엔진의 `baselineFuelM3`(코칭 미적용 가정 연료 = 반사실/counterfactual)를 활용.** 실측 `fuelM3`과의 차이가 유가·날씨 제거된 **서비스 귀속 순효과**. 라이브 −4.9%.
  - **A/B 인과 지문**: 페르소나 A(모범)2.86% < B(평균)5.25% < C(개선대상)6.34% — 개선여지 큰 군에서 효과 큼 = 코칭이 실제 원인이라는 증거. 전부 엔진 라이브 파생.
  - 신뢰지표 테이블(성과/출처/표본/교차검증), 4단 게이트(Baseline✓·Attribution✓·Verification 진행·Honesty✓), "성과 검증 안 되면 과금 안 함" 메시지.
  - recharts에 isAnimationActive={false}, 테이블 overflow-x 래퍼, grid 반응형 — 기존 교훈 전부 선반영. 검증: 빌드·DOM·라이브 폴링(A<B<C, 순효과 라이브)·콘솔 0.
- **전략 아티팩트**(별도): https://claude.ai/code/artifact/a0bbc371-17a5-4a5d-be46-9f38123ccb64 (데이터·AI·온톨로지·서비스화·성과증명 7섹션).

## 노선 관리(dash R) 복원 — 감사 누락 발견 (2026-07-14)
사용자 지적: "노선 관리는 왜 뺐어?" — **감사 도메인 누락이 원인이었음을 확인·인정**. B그룹(🚌 차량 관리)·C그룹(👥 기사 관리)은 원본 "운영관리 3종"(V/D/R) 중 V·D만 대칭 복원했고, **R(노선 관리)은 애초에 감사 워크플로 도메인 목록에서 빠졌었다**(당시 도메인: 관제/시티·차량관리·운전자관리 3개뿐, 노선관리 미포함). policyProps(정책 제안)만 노선 관리 페이지 안에서 떼어내 PolicyReport로 옮기면서 페이지 전체가 이관된 것으로 착각 — 실제로는 KPI 4카드·차트 2개·노선별 통계 테이블이 통째로 누락 상태였음.
- **신규 `src/views/operator/RouteRegistry.tsx`** = 관제 11번째 서브탭 "🛣️ 노선 관리"(drivers 다음, V→D→R 원본 순서 유지). 엔진/타입 확장 0.
  - KPI 4카드: 운영노선(128개 정적)·일평균운행·평균정시율·저효율노선, 전부 실증 3노선 라이브 각주 병기.
  - **실증 노선 라이브 브리지**: 엔진 실제 3노선(급행1/순환2/급행3)의 배차차량·정시율(CityDashboard 노선평가 패널과 동일 정적값 [96.2,93.8,95.1] 재사용, 단일소스)·평균안전점수(라이브)·연비효율(라이브, distanceKm÷fuelM3)·위험운전(라이브).
  - 노선 효율 바차트(라이브, isAnimationActive=false) + 노선별 통계 예시 대장(정적 5행, overflow-x 래퍼).
  - PolicyReport와 콘텐츠 중복 없음 — 헤더에 "정책 제안은 시티 대시보드에서" 텍스트 참조만(크로스탭 onNavigate 미배선이라 죽은 버튼 대신 텍스트로).
  - 검증: 빌드 통과. DOM 5섹션 전부 렌더·라이브 폴링(연비효율 실측 채워짐)·375px 오버플로 0·콘솔 0.

## 데이터 관리자 탭 신규 (2026-08-14)
사용자 요청: "Qdrive에서 AX 데이터 체계를 구축할 수 있다를 보여줄 수 있어야 한다 — 흩어진 데이터를 모으고 분석하고 온톨로지화해서 AI-Ready Data를 만들어 서비스와 연결".
- **신규 `src/views/AdminConsole.tsx`** = 최상위 탭 "🔗 데이터 관리자"(report 다음, roadmap 앞). 5단계 스텝 내비로 구성:
  ① 수집 — 원천 14종 커넥터(1차 8 실시간 / 2차 3 / 3차 3), 카드마다 상태·주기·필드수·보유주체·**라이브 수집량**(스냅샷 파생) + 5단 수집 파이프라인
  ② 품질 — 6룰(Q1 결측·Q2 물리 일관성·Q3 값 범위·Q4 시각 정합·Q5 위치 이상·Q6 중복) 통과율/격리 큐. Q2·Q3은 **snap.vehicles 실검사**, 나머지는 결정적 표본 비율. + DTG×RTK×BIS 3중 교차검증
  ③ 온톨로지 — 운행 단위(Trip) 허브 + 8스포크 **SVG 그래프**(인스턴스 수 라이브), 클래스별 막대, 관계 목록, "표만 있을 땐 답할 수 없던 질문" 3종
  ④ AI-Ready — 데이터셋 6종(행 라이브·피처수·**라벨 출처**·갱신주기·사용 서비스·품질점수), 스키마 고정(위험운전 8종=피처 축), AI-Ready 6조건, **AX 성숙도 L0~L5**(현재 L4)
  ⑤ 서비스 연결 — 원천→데이터셋→화면 **계보 8행**, 거버넌스 5항목(개인정보·보존·권한·감사·불이익 결정 비자동화), 2·3차 확장
- **모든 수치는 `useSim()` 스냅샷 파생** — 배속을 올리면 수집량·온톨로지 인스턴스·데이터셋 행이 실제로 증가. recharts 미사용(순수 SVG/CSS)이라 라이브 리렌더 애니메이션 취소 함정 없음.
- 함정 회피: 동적 Tailwind 클래스(`bg-${tone}-400`) 금지 → 인라인 style로 색 지정. JSX 줄바꿈 뒤 텍스트는 `{' '}` 없으면 공백이 사라짐.
- **이모지 글리프 주의**: 🗄️(U+1F5C4)는 이 환경 Chrome에서 tofu로 렌더 → 🔗로 교체. 새 이모지는 캡처로 실렌더 확인할 것.
- 검증: 빌드 통과 / 라이트·다크 렌더 / 375px 가로 오버플로 0 / 5스텝 DOM·SVG(노드 9·엣지 8) 확인. 커밋 4b33b2c, 배포 success.

### 원천별 통합 관리 콘솔로 보강 (2026-08-14, 커밋 0e26dc0)
사용자 요청 "실제 각 원천별 데이터들을 통합 관리한다고 생각하고 상세하게" → 카드 나열을 **마스터-디테일 운영 콘솔**로 재구성. `src/views/admin/` 분리:
- `catalog.ts` — 원천 14종(스키마 필드·지연·실레코드 샘플 생성기) · 품질 룰 6종(격리 샘플 포함) · 온톨로지 9클래스(속성·인스턴스 샘플·질의 3종) · 데이터셋 6종(피처 정의·샘플) · 계보. **화면은 이 정의만 읽는다** — 실단말 전환 시 count/sample만 교체
- `integration.ts` — 원천별 **연동 설정**(엔드포인트·인증·재시도·중복제거 키) · **조인 키** · **정규화 규칙** · **필드 매핑(원천→표준→온톨로지)** · 스키마 버전 이력 · 미연결 원천 준비/차단 상태 · 수집 로그 · 24h 추이
- `ui.tsx` — Drawer(ESC·배경 클릭) · Spark · RecordTable · Search · Chips · LogRow 등 공용
- 화면 6종: Ingest(표+드로어 4탭) / Quality(원천×룰 매트릭스·격리 드로어·**재처리 실동작**) / Ontology(노드 클릭 드로어·로그 스케일 막대·질의 실계산) / Datasets(정렬·드로어·서비스 딥링크) / Lineage(계보 딥링크·**영향도 분석**) / Operations(처리 작업·저장·감사·SLA)
- **재처리 상태는 AdminConsole이 보유** — 품질 패널에서 누르면 총괄 KPI·알림까지 함께 반영(62→0 검증)
- 함정: 동적 Tailwind 클래스 금지(인라인 style) · JSX 줄바꿈 뒤 텍스트는 `{' '}` 필요 · 한글 줄바꿈은 `break-keep`
- 검증: 빌드 통과 / 드로어 4탭·매핑·격리샘플·온톨로지 노드·데이터셋·영향도·운영 DOM 확인 / 딥링크 이동 / 라이트·다크 / 375px 오버플로 0 / pageerror 0. 배포 success.

## 회차별 연료·CO₂ 오류 수정 (2026-08-14)
`engine.completeTrip`이 **누적** `fuelM3`를 회차 기록에 그대로 써서, 회차를 거듭할수록 연비가 1.6→0.9→0.6 km/L로 나빠 보이고 CO₂는 20→87kg로 늘어나던 오류. `tripStartFuel` 필드를 추가해 **구간값**만 기록하도록 수정(기사앱 운행 기록·운수사 운행 이력 공통). 함께 DriverApp 리포트의 평균 연비 3.3 km/L를 앱 정본(실증 리포트 2.42→2.53)과 맞춰 2.5로, 주간 연비 추이 데이터·Y축도 같은 축으로 정렬. 커밋 2af7a6c.

### 그래프 렌더 원칙 · 계보 데이터 맵 (2026-08-14, 커밋 3a1cd28)
사용자 지적: "선이 원 위에 보이면 안 돼" / "더 구축된 형태로" / "데이터 리니지도 결국 온톨로지 데이터 맵 형태로 관리할 수 있는 것 아닐까?"
- **SVG 그래프 렌더 원칙(재사용)**: 노드마다 `<line>+<circle>`을 함께 그리면 **뒤 노드의 선이 앞 노드의 원을 덮는다.** 반드시 레이어를 분리해 `엣지 → 라벨 → 노드` 순서로 그릴 것. 엣지는 `graph.ts`의 `trim(a,b)`로 두 원 경계에서 끊어 원 안으로 들어가지 않게 하고, 라벨 겹침은 엣지별 `at`(0~1) 위치 + `paintOrder: stroke` halo로 회피.
- **온톨로지 확장**: 클래스 9 → **11**(WorkOrder 정비 작업지시 · Plea 상황 설명 — 둘 다 학습셋 라벨 공급원이라 온톨로지에 있어야 함), 관계 8 → **13**(관계명 표시, 실선=중심축 직접 / 점선=클래스 사이 파생 + 범례). 노드 hover 시 걸린 관계만 강조. 좌표·관계는 `src/views/admin/graph.ts`에 분리.
- **계보를 데이터 맵으로**: 표 → **3열 노드-링크 맵**(원천 13 · 데이터셋 8 · 서비스 화면 7 · 간선 35). 베지어 간선도 노드보다 먼저 그림. 노드 선택 시 상·하류 전체 경로가 살아나고(양방향 BFS) 나머지는 흐려짐. 상세 패널이 계보·영향도·대체 경로를 함께 표시, 서비스 노드 두 번 누르면 해당 탭으로 이동. **온톨로지와 계보를 같은 시각 언어로 통일** — "모든 걸 그래프로 관리한다"는 메시지.
- 검증: 빌드 통과 / SVG 레이어 순서(lines→labels→nodes, edges→nodes) DOM 확인 / 클래스 11·관계 13 / 계보 노드 28·간선 35 / 선택·딥링크 / 375px 오버플로 0 / pageerror 0. 배포 success.

### 인스턴스 탐색기 — 레코드 사이를 걷는 그래프 (2026-08-14, 커밋 c8fb1ae)
사용자 요청 "데이터간의 연결, 연결이 더 느껴지면 좋겠다". 클래스+건수만 보여주던 그래프는 연결이 추상적이라, **실제 레코드 사이의 연결**을 걷는 탐색기를 추가.
- `instance.ts` — 스냅샷에서 이웃 관계를 만드는 그래프. id 규약 `trip:<vid>:<start>` `veh:` `drv:` `rt:` `stop:` `evt:` `sns:` `loc:` `ctx:` `wo:` `plea:`. 11종 모두 중심이 될 수 있고 각자 실제로 걸린 레코드를 이웃으로 반환.
- `Explorer.tsx` — 방사형 인스턴스 그래프. 노드 클릭 시 재중심(순회) · **지나온 경로 breadcrumb** · **트리플 목록(주어 ─관계→ 목적어)** 클릭 이동 · 시작점은 실제 쌓인 운행/차량/이벤트.
- 사각 노드는 `edgePoint()`로 경계에서 선을 끊음(원은 `trim()`). 렌더 순서 규칙은 동일.
- 클래스 그래프 관계 목록에 **실제 연결 건수** 표시.
- 검증: 3단계 순회(운행 6690호 → 차량 6690호 → 노선 급행3) · breadcrumb · 트리플 · 레이어 순서 · 375px 0 · pageerror 0. 배포 success.

## 온톨로지 스튜디오 → 별도 저장소로 분리 (2026-08-14)
**이 저장소에는 더 이상 없다.** 사용자 지시로 `qdrive-ontology` 독립 저장소로 이관 — 운영 플랫폼은 이해관계자별 화면이 축이고 온톨로지 스튜디오는 문법·인과가 축이라 성격이 다르고, 여기 두면 최상위 탭이 11개가 되어 "누가 보는가" 단일 축이 흐려진다.
- 저장소: https://github.com/hyegeungim3-git/qdrive-ontology · 라이브 https://hyegeungim3-git.github.io/qdrive-ontology/
- **시뮬레이터 코어(`src/sim/`)는 양쪽에 복제**돼 있다. 엔진을 고치면 두 저장소 모두 반영할 것.
- 🔗 데이터 관리자(파이프라인)는 이 저장소에 그대로 남는다 — 온톨로지 ③단계(인스턴스 탐색기 포함)도 여기 유지.

<details><summary>분리 전 이력 (커밋 04c1bb4 · 참고용)</summary>

사용자 요청: OpenCrab(github.com/AlexAI-MCP/OpenCrab, MetaOntology OS MCP 플러그인) 정밀 분석 후 "A안 + lever 시뮬레이터를 별도 메뉴로. 기존 관리자 시스템은 그대로".
- **분리 원칙**: 🔗 데이터 관리자 = 파이프라인(어떻게 들어오나) / 🧭 온톨로지 스튜디오 = 의미 구조(무엇을 뜻하고 무엇을 움직이나). 두 탭 모두 유지.
- **왜 필요했나**: 기존 온톨로지는 클래스 11종이 관측·자산·주체 3개 자리에만 몰려 있고 **판정·성과·조치가 통째로 비어** 있었다. 그 데이터(justified·score·recommendations·workOrders)는 앱에 이미 있는데 노드로만 안 올라와서 "AI가 왜 그렇게 판단했나"를 그래프로 답할 수 없었다.
- **`src/views/ontology/meta.ts`** — 9 스페이스(규정·자산·주체·관측·개념·판정·집단·성과·조치) × 노드 타입 35종, 메타 엣지 11방향 × 관계 어휘 30종. 인스턴스 수는 전부 스냅샷 파생. **핵심 사슬: 관측 ─뒷받침→ 판정 ─반영→ 성과 ←올림─ 조치**
- **`SpaceGraph.tsx`** 스페이스 그래프(렌더 순서 규칙 준수·클릭 시 드로어) / **`Grammar.tsx`** 문법표+어휘 사전 / **`Simulator.tsx`** 조치 시뮬레이터
- **조치 시뮬레이터**: 조치 선택 → 강도 슬라이더 → 연결된 성과의 현재/예측/변화/근거/신뢰도. 계수는 엔진 값 출발(연료절감=baselineFuelM3 반사실, 배차=headway 실집계). **근거 유형별 신뢰도 상한(실측 95 / 환산 85 / 추정 70 / 정성 50)** — 모르는 것을 아는 척하지 않게 막는 장치. 영향 범주 I5·I7 표기.
- **OpenCrab에서 가져온 것은 개념 체계뿐 — 코드 복사 없음**(LICENSE 파일 없음 = All rights reserved). 스페이스 이름도 영어 그대로가 아니라 우리 도메인 용어로 재정의. 도입하지 않은 것: Neo4j·Chroma·MCP 서버·ReBAC 엔진·크롤러 워커(백엔드 없는 데모 원칙과 충돌), d3-force(11~35 노드 규모엔 고정 좌표가 안정적).
- **레이아웃 함정**: 짧은 가로 간선의 관계 라벨은 노드가 나중에 그려져 양끝이 잘린다 → 노드 간 가로 간격을 라벨 폭 이상으로 벌릴 것(자산↔관측 220→270px).
- 검증: 빌드 통과 / 스페이스 9·간선 11·레이어 순서 / 판정 드로어 / 문법표 11행 / 시뮬레이터 강도 50→100% 시 연료절감 5.74→6.59%·신뢰도 70→95%, 예지정비 정비비 340→204만원 / 375px 오버플로 0 / pageerror 0. 배포 success.
</details>

## 화면 용어 정책 — 음차·약어 전수 정비 (2026-08-19)
사용자 지적("'잡'이라는 단어가 헷갈린다 → 모든 화면 쉽게 읽히게"). **화면 텍스트 전수 검사 방법**: 브라우저에서 전 탭·서브탭을 순회하며 `document.body.innerText`를 모아 코퍼스를 만든 뒤 사전 대조(코드 grep은 식별자·주석이 섞여 오탐이 많다). 25개 화면 코퍼스 기준으로 판정.

**3단 원칙** (새 문구를 쓸 때 이 기준을 따를 것):
1. **교체 — 순수 IT 은어**: 잡→처리 작업 · 룰→규칙 · 큐→격리함/보류함/대기열 · 게이트→관문 · 스키마→데이터 형식 · 피처→학습 항목 · 레코드→기록/데이터 · 타입→형식 · 엔드포인트→접속 주소 · 커넥터→연결(부) · 스트림→실시간/주기 · 백오프→재시도 · 폴링→조회 · p50/p95→중앙/상위5% · 매트릭스→대조표 · 라벨→정답 라벨
2. **병기 — 개념 자체가 제안의 핵심이라 유지 + 우리말 풀이 1회**: AX→`AI 전환(AX)`, SLA→`응답 목표(SLA)`. 온톨로지·AI-Ready·데이터셋·학습셋은 화면에 이미 풀이 문장이 붙어 있으므로 그대로 유지.
3. **유지 — 도메인 표준 약어와 정착 외래어**: DTG·OBD·CAN·RTK·BIS·AFC·APC·BMS·CNG·V2G·KOC·eTAS(대부분 이미 괄호 병기됨) / 리포트·대시보드·에이전트·시뮬레이터·코칭·인사이트·랭킹·샘플·매핑.
- **주의**: '작업' 단독은 정비 **작업지시**(WorkOrder)와 헷갈리므로 파이프라인 job은 반드시 **'처리 작업'**. '오큐브'(사명)는 '큐' 치환 대상이 아니다.
- 데이터셋 이름은 `catalog.ts`의 문자열이 계보 그래프 노드 라벨과 **같은 키**로 쓰인다(`LINEAGE.ds` ↔ `DATASETS.name`) — 한쪽만 바꾸면 계보 간선이 끊긴다. `'...피처'` → `'...학습셋'` 4종은 replace_all로 동시 치환함.
- 검증: 빌드 통과 / 신규 라벨 22종 전부 DOM 렌더 확인 / 드로어(원천 4탭·격리·데이터셋) 실클릭 확인 / 고장 트리거로 격리 표 헤더 '규칙' 라이브 확인 / 계보 SVG 노드 라벨 4종 확인 / pageerror 0(폰트 404는 dev 서버 기존 이슈).

## 💬 AI 채팅 탭 + 운행유형·차고지 축 (2026-08-19)
제안서 슬라이드 4가 든 4개 질문("맥락이 붙으면 비로소 답할 수 있는 질문")에 프로토타입이 **실제로 계산해 답하도록** 만든 작업. 같은 슬라이드가 「1회 운행」을 9항목(날짜·회사·차량·기사·노선·방향·순번·출발도착·**운행유형**·**차고지**)으로 정의하는데 엔진에 뒤 두 축이 없어 2개는 답이 불가능했다.

**엔진 확장** (`sim/types.ts`·`engine.ts`·신규 `sim/depots.ts`)
- `Packet521`에 `kind('영업'|'공차')·direction·seq·depot·company` / `Packet409`에 `routeName·weather`
- 차고지 3곳(성서=세운·북부=경북·칠곡=신흥) + 편도 회송거리 **예시 상수**. 공차 발생 = 시작 시 전 차량 출고 1건 + 영업 `DEADHEAD_EVERY_N_TRIPS`(4)회차마다 입·출고 쌍
- **공차는 `snap.deadheads`로 분리 보관.** `snap.trips`(영업)에 섞으면 운행이력·연비·정산·데이터셋 행수 등 20곳 넘는 소비처가 오염된다. 차량 누적(`distanceKm`/`fuelM3`)에도 더하지 않는다 — 기존 수치 회귀 0을 위해서다.
- **이벤트에 날씨를 새기는 이유**: 스냅샷은 현재 날씨만 들고 있어, 과거 이벤트의 맥락을 사후에 물으면 답이 틀린다. 검증에서 현재 폭우일 때 맑음 이벤트 122건이 그대로 유지되는 것을 확인.

**질의 엔진** `sim/ontologyQa.ts` — **두 저장소 공유 파일**(qdrive-ontology에도 복제). 답 + `path`(순회한 클래스·관계) + `evidence`(근거 수치) + `caveat`(예시 상수 고지) + `empty`(데이터 없음) 반환. 키워드 라우팅이라 **네트워크 없이 동작** — 시연 중 인터넷이 죽어도 4개 질문은 답이 나온다.
- **키워드는 좁게 유지할 것.** '왜'·'노선'·'주행'·'유형'을 넣었다가 "안전점수는 **왜** 그 점수인가"·"724번 **노선**을 증차해야 합니까" 같은 기존 질문을 가로챘다. 제거함.

**화면** `views/OntologyChat.tsx` = 최상위 탭 **'💬 AI Q'**(부제 '온톨로지 근거 질의', admin 다음). **이름은 두 저장소 공통 'AI Q'** — qdrive-ontology의 진입 버튼·패널 제목도 같다. 운영 플랫폼에는 플로팅 코파일럿(`components/Copilot.tsx`)도 '✨ AI Q'라 **같은 브랜드의 두 진입점**이다(오버레이=어느 탭에서든 빠른 조회, 탭=근거를 펼쳐 보는 본 화면). 아이콘 💬/✨로 구분. 파일명은 OntologyChat 그대로 — 화면의 정체가 «온톨로지에 근거한 채팅»이라서다. **용어 주의: 「온톨로지 기반으로 LLM을 만든다」는 부정확하다.** 모델은 학습이 끝난 범용 Claude이고 온톨로지는 답에 근거를 대는 구조다. 제안서 표현은 '온톨로지가 AI의 답에 근거를 댄다' / '온톨로지 위에서 도는 AI 서비스'로 쓸 것. 규칙 경로가 기본이고, 자유 질문만 라이브 Claude(`claude-opus-5`, 사용자 키 localStorage)로 넘기되 그 답에는 **'계산이 아니라 설명'** 꼬리표를 붙인다. 못 알아들으면 지어내지 않고 무엇이 있으면 답할 수 있는지 말한다.
- Anthropic 호출은 저장소 관례대로 raw fetch(백엔드 없음). 사고는 켠 채 `effort: 'low'`로 지연만 낮추고, `stop_reason === 'refusal'`을 **content 읽기 전에** 확인. `fallbacks: 'default'` 옵트인.
- 참고: 기존 `components/Copilot.tsx`는 아직 `claude-opus-4-8`을 쓴다(이번 범위 밖).

**검증**: 두 저장소 빌드 통과 / 4개 질문 전부 라이브 계산값 확인(영업6회 34.9km vs 공차9회 43.5km, 차고지별 집계, A 2.85%<B 5.26%<C 6.32% 인과 지문) / 자유 입력 라우팅·모르는 질문 처리 / 운행 이력에 공차 미혼입 확인 / 375px 오버플로 0 / 콘솔 에러 0.

**qdrive-ontology 쪽**: 기존 `AgentChat`에 kind `'onto'` 추가(도구 호출 뷰에 `graph.walk`·`engine.aggregate`가 찍혀 "지어낸 게 아니다"가 보이고, 보고서 절로도 넘어감). MORE 칩의 '공차 거리를 알 수 있나요?'(gap)를 4개 질문으로 교체. **라우팅은 `ONTO_SIGNAL` 정규식**(공차·영업·차고지·회송·유가·급감속+날씨)으로 좁혀 정책/안전 시나리오를 가로채지 않게 함 — 회귀 확인 완료.
### AI Q 신뢰 장치 3종 + 화면 상태 보존 (2026-08-19)
사용자 요청 "① 교차검증 표시 ② 한계에 «이 데이터가 붙으면 답할 수 있다»를 눌러 로드맵으로 ③ 답 전체를 보고서로 내보내기" + "새로고침해도 처음 대시보드로 돌아가지 않게".
- **교차검증(`QaCross`)**: 답마다 «서로 다른 원천이 같은 사실을 말하는가»를 실제로 계산해 통과/주의로 표시. 예) `DTG 521(주행거리) × OBD/CAN(연료 실측)` — 회차 연비가 CNG 물리 범위(0.8~4.0 km/m³) 안인가 / `DTG 409(이벤트 시각) × DTG 521(회차 구간)` — 이벤트가 그 회차 구간에 귀속되는가 / 귀속 답에서는 회차합 vs 차량누적 연료, 기준선 > 실측. **한 원천만으로 나온 수치는 검증이 아니다** — 교차할 짝이 없으면 절을 만들지 말 것.
- **한계 → 로드맵 딥링크(`QaLimit.unlock`)**: `limits`를 문자열에서 `{text, unlock}`로 승격. `unlock`이 있으면 «AFC·APC 붙으면 → 로드맵» 버튼이 붙고 `onNavigate('roadmap')`. 못 하는 걸 말하고 끝내지 않고 «무엇이 붙으면 되는가»로 잇는다.
- **보고서 내보내기**: `buildReport(msgs, snap)`가 대화 전체를 Markdown으로(결론·신뢰도·집계구간·절·근거표·교차검증·원본 레코드·순회 경로·한계·출처). 화면에만 있던 신뢰 장치를 문서에도 그대로 싣는다 — 안 실으면 그 장치는 장식이다. Blob 다운로드(클립보드는 권한에 막히는 일이 있어 저장이 기본).
- **새로고침 시 탭 유지**: `App.tsx`에 `tabFromHash()` + lazy `useState` 초기화 + 양방향 동기 `useEffect` 2개. 탭 전환은 `replaceState`라 뒤로가기 이력이 더럽혀지지 않는다. `#citizen`은 앱 셸 밖 독립 진입점이라 예외 처리.
- **대화 보존**: OntologyChat의 `msgs`를 모듈 스코프 `kept`에 미러. 답 안의 로드맵 링크를 누르면 탭이 바뀌며 언마운트되는데, 근거를 확인하러 간 대가가 대화 손실이면 안 된다. 복원 시 `role==='thinking'`은 버린다(스트리밍 중 이탈하면 영영 끝나지 않는 거품이라서).
- **조사 자동 선택(`josa`)**: 값이 데이터에서 오는 문장은 조사가 틀어진다 — «차고지 출입고이(가)», «영업»로. 한글 마지막 글자 받침 `(code-0xAC00)%28`로 이/가·으로/로를 고른다. **문자열에 «이(가)» 같은 괄호 병기를 남기지 말 것**이며, 값이 변수인 자리의 조사는 전부 이 함수를 거칠 것.
- **화면 용어 정책은 신규 화면에도 적용**: AI Q가 정책 수립 후에 만들어져 «레코드»가 새로 유입됐다. 원본 레코드→**원본 기록**, 레코드 N개→**기록 N건**. 새 화면을 만들 때 위 «화면 용어 정책» 절을 다시 읽을 것.
- **내보낸 문서는 열어서 읽어 볼 것**: 화면에서 안 보이던 결함(조사·작성 시점·용어)이 문서에서 드러난다. 제출 문서의 «작성 시점»은 실제 날짜여야 하고(시뮬 시각은 경과 시간으로 병기), 헤더 누적치는 «내보낸 시점 기준»임을 밝혀 절별 집계 구간과 어긋나 보이지 않게 한다.

### 보고서 2형식·2범위 + 교차검증 보강 (2026-08-19)
- **범위 2개**: 헤더 `📄 전체 보고서 (N)` = 대화 전체 / 답마다 `📄 이 답만` = 그 답 하나. **라벨만 보고 범위가 구분돼야 한다** — 체크박스 선택 모드는 두지 않았다(시연에서 «어떻게 고르나»를 한 번 더 설명해야 해서다). 두 경로 모두 `collectItems(msgs, onlyId?)` 한 곳을 지나므로 형식이 갈라지지 않는다.
- **형식 2개**: 인쇄용 HTML(주) + 텍스트 .md(부, «한글·메일에 붙일 때»). HTML은 근거 수치·교차검증·원본 기록을 **표**로 되돌린다 — 줄글로 흐르면 대조가 안 된다. `@page A4`·`break-inside: avoid`·`.bar { display:none }` 인쇄 CSS 포함, 화면색이 아니라 흰 종이 기준 팔레트로 다시 짬.
- **새 탭으로 연다(`openReport`)**: 시연에서 «다운로드 폴더를 뒤지는 시간»이 가장 설명하기 어렵다. 새 탭 상단에 «🖨 인쇄 · PDF로 저장» 버튼(인쇄 시 자동 숨김). 팝업 차단 시 .html 파일 저장으로 폴백.
- **교차검증 4항목으로 보강**: 이벤트 맥락 +기상 공통성(같은 시각 다른 차량도 같은 날씨인가)·+기사 소명 정합(짝이 있을 때만) / 차고지 +편도거리 대조(1건당 거리 vs 기준값)·+운수사 소속 참조 무결성 / 성과 귀속 +이벤트↔점수 방향 일치·+기사군 순서(A<B<C). **대조할 짝이 없으면 항목을 만들지 않는다** — 소명이 없는 것은 결함이 아니다.
- **«정합»이라고 덮지 말 것**: 회차합 vs 차량누적은 원래 같을 수 없다(진행 중 회차·공차 분리 보관). 차이를 숨기고 정합으로 적었다가 «회차합 112 · 누적 143 — 정합»이 되어 오히려 신뢰를 깎았다. 지금은 `회차합 + 진행 중 = 누적` 형태로 잔차를 드러내고 «회차 하나 분»을 넘는지로 판정한다.
- **메시지 id는 복원본 뒤에서 이어 붙일 것**: 대화 보존(`kept`)과 모듈 `seq`가 따로 초기화되면 번호가 겹쳐 «이 답만»이 남의 답까지 집는다(HMR에서 실제 재현). 복원 시 `seq = max(id)+1`.
- 검증: 빌드 통과 / 범위 분리(이 답만=1문항·3표 / 전체=2문항·5표) / HTML 구조(section·표·인쇄버튼·@page) / iframe 실렌더 가로 오버플로 375·768·1280 전부 0 / 교차검증 항목수 이벤트 3 · 차고지 4 · 귀속 4, 전 항목 실수치 통과.
- **「이 답이 말할 수 없는 것」 절은 미표시(2026-08-19, 사용자 요청 «일단 빼줘»)**: 화면·인쇄 HTML·텍스트 세 곳에서 함께 뺐다(한 곳만 빼면 문서와 화면이 어긋난다). 머리말·꼬리말의 «한계를 함께 적었습니다» 문구도 같이 고쳤다 — **없는 것을 있다고 말하는 문서가 되면 안 된다.** 데이터 `QaResult.limits`와 `unlock`은 엔진에 그대로 남아 있어 렌더만 되살리면 즉시 복구된다. **딸려 나간 것: ② 한계→로드맵 딥링크 버튼**(그 절 안에 있었다). 되살릴 때 같이 돌아온다.
- **AI Q 어조 — 「다 된다」로 보이게 (2026-08-19, 사용자 요청)**: 발표자가 부족한 부분을 말로 설명하므로, 화면이 스스로 결함을 앞세우지 않게 고쳤다. **없는 기능을 있다고 적지 않는 선은 지켰다** — 근거의 «종류»를 바꿔 말했을 뿐 «실집계»와 «상수»의 구분은 그대로다.
  - 신뢰도 `실측/환산` → **`실측 기반 / 기준정보 결합`**, «상한» 표기 삭제(신뢰도 85%). 경고색(amber) → sky.
  - 출처 «예시 상수 · 실증 시 실측 교체» → **«운수사 기준정보»** (차고지 편도거리는 실제로 운수사가 정의하는 기준값이라 사실에 더 맞다). 원본 기록 필드명도 동일.
  - 데이터가 아직 안 쌓인 순간의 제목을 **있는 것 중심**으로: 「아직 완료된 영업 운행이 없습니다」 → 「지금은 차고지 출고 기록부터 쌓이고 있습니다」.
  - 못 알아듣는 질문: 「답할 수 없습니다」 → **「왼쪽 네 질문은 지금 바로 계산해 답합니다」**(할 수 있는 것을 먼저).
  - 사이드바 `○ 미연결` → `설정`. 서비스가 죽은 것처럼 읽히던 표시 제거.
  - 라이브 Claude 지침도 «아직 연결되지 않은 축» → «이번 실증 범위 밖(수치를 지어내지 말 것)» — 모델이 스스로 결핍을 길게 말하지 않게.
  - **남아 있는 결핍 표기(다른 화면)**: 데이터 관리자 «연결 대기» 5곳, 운수사 «예시 대장» 4곳, 로드맵 «예정» 9곳. 이들은 각각 «2·3차 확장 서사»·«스케일 오도 방지»·«로드맵 그 자체»라 의미가 있어 이번 범위에서 건드리지 않았다. 함께 바꿀지는 사용자 판단.

### 온톨로지 정합 — 정의와 순회를 같은 어휘로 (2026-08-19)
사용자 확인 요청("qdrive-ontology에서 관리되는 데이터랑 상호 연결이 잘 되어있지?")에 실제 대조 → **갭 발견 후 정합**.
- **진단**: 공유 `src/sim/` 8파일은 두 저장소 바이트 동일(엔진은 한 몸)인데, **선언된 온톨로지만 뒤처져** 있었다. AI Q 순회는 `dep:`·`co:` 노드를 만들어 답하는데 ①unified 데이터 관리자 ONTO 11클래스에 차고지·운수사 없음(운수사는 `operatedBy → Operator` 속성 참조만) ②ontology 스튜디오 catalog/shacl/grammar에 차고지·공차·운행유형 0건. 역방향으로 AI Q 순회에는 센서·위치·정비가 없어 「이 고장은 어느 회차에서 났나」를 걷지 못했다.
- **unified**: ONTO 11→**13**(차고지 Depot·운수사 Operator), 관계 13→**16**(소속차고지·운영주체·보유차량), 운행 클래스에 `kind(운행유형)`·`fromDepot`·`direction/seq` 선언, 운행 인스턴스 수 = 영업+공차. 인스턴스 탐색기에 `dep:`·`co:` 분기 추가(차고지→운수사·담당노선·공차/영업 운행).
- **AI Q 순회**(`sim/ontologyGraph.ts`): `sns:`(센서 측정)·`loc:`(위치 관측)·`wo:`(정비 작업지시) 추가. 실제 확인 — 3742호에서 «탑재센서→냉각수온 112℃ →정비이력→ 냉각수온 이상(초안)»까지 걸린다.
- **스튜디오**(`ontology/meta.ts`): 자산에 차고지, 개념에 운행유형, 운수사를 `live: true` 실집계로, 운행 기록 수를 영업+공차로. 노드 타입 52→**55종**.
- **회차 노드 id 충돌 수정(중요)**: `trip:<차량>:<시작시각>`이 키였는데 **영업 회차와 공차가 같은 시각에 시작하면 한 노드로 합쳐져**, 순회 결과에 남의 관계가 섞였다(«순환2 2회차 ─운행노선→ 북부차고지 출고»). `tripKey()`로 **순번까지** 넣어 분리. 같은 이유로 AI Q 공차 대상 id에도 `seq` 추가(React 중복 key 경고로 드러났다).
- 부수 수정: 탐색기 시작점에 공차·차고지 포함(시작 직후엔 공차만 쌓여 시작점이 비어 있었다) · 표시 노선명이 `R1`로 나오던 폴백을 노선 기준정보 이름으로.
- 검증: 두 저장소 빌드 통과 / 클래스 13·관계 16 DOM 확인 / **SVG 선-노드 겹침 0**(13노드×16선 기하 검사) / 차고지 시작점 → 운영주체·담당노선·공차 운행 순회 / AI Q 순회에 센서·위치·정비 등장 / 스튜디오 자산 5종에 차고지 LIVE 3.

### 검증 규칙(SHACL) — 차고지·운행유형까지 «실제로 막히게» (2026-08-19)
- **왜**: 규칙이 없으면 틀린 값이 «그럴듯한 답»으로 흘러간다. 이번에 실제로 겪은 것 — 회차 노드 id 충돌로 AI Q가 「순환2 2회차 ─운행노선→ 북부차고지 출고」를 자신 있게 출력했고 에러도 경고도 없었다. 사람이 눈으로 읽어야 발견되는 결함을 적재 시점에 기계가 잡게 하는 장치가 검증 규칙이다.
- **추가한 제약**(qdrive-ontology `standards.ts` → `shacl.ts` 자동 생성): 운행유형 `sh:in(영업·공차)` · 운행 순번 `min 1` · 차고지명 `sh:in(등재 3곳)` · 편도거리 `0.5~30km` · 운수사 상호명 필수 / 클래스 규칙 2개 — 회차의 소속차고지 `minCount 1 maxCount 1`, 차고지의 운영 운수사 `minCount 1 maxCount 1`.
- **함정 ①: 관계의 `required`는 스페이스 전체에 걸린다.** REL_META에 `소속차고지 required:true`를 두니 이벤트·센서·날씨까지 차고지를 요구받아 **정상 데이터가 71건 위반**으로 잡혔다. 클래스 단위 도메인 규칙(4절, `RuleTripNeedsDepot`)으로 옮겨야 한다.
- **함정 ②: 노드 타입의 스페이스를 확인할 것.** 운수사(Operator)는 «집단(Community)»에 선 타입인데 «주체»로 세웠더니 닫힌 셰이프의 허용 속성 목록이 달라 `fleetSize`가 sh:Closed 위반으로 잡혔다.
- **함정 ③: 결함을 눌렀는데 아무 일도 안 일어나면 규칙이 없는 것처럼 보인다.** 회차를 건드리는 결함은 영업 회차가 쌓이기 전엔 대상이 없다 → `needsTrips` 조건에 새 결함도 넣어 보충 회차를 만든다.
- **규칙이 실제로 잡아낸 것(선물)**: `Plea.method` 스펙이 `앱·전화·대면`인데 엔진은 `음성·버튼`을 기록하고 있었다. 스펙과 데이터가 처음부터 어긋나 있었고 아무도 몰랐다 — 제약을 켜자 2건이 즉시 걸렸다. 스펙을 엔진에 맞춰 교정.
- **결함 주입 4종 추가**(⑨ 규칙 검사): 운행유형 표준 밖 값 · 미등재 차고지명 · 소속차고지 누락 · 편도 회송거리 이상. 전부 눌러서 걸리는 것을 확인했고, 걸린 건은 ⑩ 격리 큐로 넘어간다.
- 검증: 기본 상태 **위반 0건** / 4종 각각 의도한 제약에 걸림(sh:In · sh:MinCount · sh:MaxInclusive) / 격리 큐 반영 확인.

### AI Q — 「묻지 않은 것」을 답에 싣지 않기 (2026-08-19, 사용자 지적)
사용자가 급출발 이벤트를 물었는데 소명 절에 **급진로변경 소명**이 실려 있었다("급출발에 대한 내용이어야 하는데").
- **원인**: 소명 절이 «차량» 기준이었다. 앞서 «두 기사가 한 건에 답한» 것처럼 보이던 버그를 차량 스코프로 고쳤는데, 이번엔 그 스코프가 너무 넓었다.
- **수정**: 절을 «이 이벤트»로 좁힘 — 차량 **AND 이벤트 유형**이 모두 맞는 소명만 싣는다. 없으면 「이 건에 대한 설명은 아직 접수되지 않았습니다」 + 다른 유형 설명은 **«이 답의 근거로 쓰지 않았습니다»라고 명시**해 참고로만. 접수 시각을 함께 적고, 이 건과 5분 넘게 벌어지면 「같은 차량·같은 유형의 이전 접수 건」이라고 밝힌다.
- **같은 계열 결함 하나 더 발견**: 성과 귀속 답의 「낭비 요인 분해」가 대상이 1대일 때도 **9대 합계**를 싣고 있었다 → 집계 범위를 대상에 맞추고 제목에 범위를 적었다(`낭비 요인 분해 — 3742호`).
- **원칙**: 답의 각 절은 «물은 대상»과 스코프가 같아야 한다. 비교·전체 수치를 실을 거면 **제목에 그 범위를 적는다**(「같은 노선과 비교」·「오늘 전체에서 차지하는 몫」처럼). 나머지 절(차고지별 비교·전체와 비교·같은 맥락 누적)은 이 기준으로 재점검해 이상 없음.

## 지도 — 실제 노선 형상·실제 정류장으로 교체 (2026-08-19)
사용자 요청: 「대표 노선을 지도 위에 정확하게 표현 · 정류장 핀 · 대구의 주요 버스들과 도로들」.
- **데이터 출처: OpenStreetMap 버스 노선 관계**(type=route, route=bus). 대구 급행1~9·순환2/2-1·356이 실제로 등록돼 있고, **지금 엔진이 쓰던 3개 노선명(급행1·순환2·급행3)이 실제 노선번호와 그대로 일치**했다. 좌표는 정류장을 직선으로 이은 선이 아니라 **버스가 타는 도로 형상**이다.
- **한 번만 받아 소스에 박는다**(`scripts/build-routes.py` → `emit-routes.py` → `src/sim/routes.ts`). 런타임에 Overpass를 부르지 않는다 — 데모는 인터넷이 끊겨도 살아야 하고 키를 커밋할 수 없다. 출처 표기: © OpenStreetMap contributors (ODbL).
- **운행 3 / 표시 3 분리**: `ROUTES`(급행1·순환2·급행3 — 엔진 차량이 실제로 달림) / `EXTRA_ROUTES`(급행4·급행6·순환2-1 — 지도 표시 전용). 표시 노선에 차량을 태우면 9대 실증 스케일이 흐려진다.
- **way 이어붙이기 함정(3연속 실패 후 해결)**: ①가까운 끝점 탐욕 방식 → 첫 way가 노선 중간이면 한쪽만 물고 끊긴다(40km→19km) ②멤버 순서를 그대로 믿음 → 다른 갈래로 건너뛰며 선이 왕복해 길이가 부풀었다(40km→124km) ③**해결: 끝점을 키로 인접 그래프를 만들어 걷고, 여러 시작점 중 가장 긴 경로 채택.** OSM way들은 실제로 같은 노드를 공유한다.
- **왕복 컷**: 이어 붙인 선은 대개 갔다가 같은 길로 돌아오는 왕복이라 끝점이 시작점과 겹친다. 그대로 두면 편도 거리가 두 배가 되고 정류장도 겹친다 → **시작점에서 가장 먼 점(반환점)에서 자른다.**
- **정류장**: 노선별로 따로 요청하면 요청 수가 많아 막힌다 → **대구 전역 bus_stop 1,365개를 bbox 한 번으로 받아** 각 노선 선형에 투영(70m 이내)·진행순 정렬·양방향 중복(「건너」/「앞」) 병합. 결과: 급행1 32개·급행3 26개·순환2 10개, 전부 실명(동구청역(4번출구)·불로화훼단지·2.28기념중앙공원…).
- **순환2는 loop:false**: OSM에 이 노선 ring이 부분만 매핑돼 끝점 간격이 3.1km다. 억지로 닫으면 도심을 가로지르는 직선이 그려진다 — **없는 도로를 그리느니 매핑된 구간만 쓴다.**
- **급행5 제외**: 이어진 도로가 4.4km뿐 — 짧게 잘린 선을 「주요 노선」이라 깔면 지도가 거짓말을 한다.
- **지도 UI**: 노선 칩으로 개별 토글 + 「+ 주요 노선 3」 한 번에 / 정류장 핀은 **줌 단계별**로(z<13 기점·종점만, z13 1/3, z≥14 전부) — 다 켜 두면 핀이 노선을 덮어 「어디를 지나는가」가 안 보인다.
- **반월당이 사라진 문제**: 실제 노선으로 바꾸니 세 노선이 모두 지나는 정류장이 없어졌다(실제 대구 노선은 도심에서 스치듯 만나고 정류장 이름이 저마다 다르다). 승객 앱의 기준 정류장을 **「가장 많은 노선이 서는 정류장」으로 자동 선택**(현재 원대오거리)하고, 그 정류장이 없는 노선은 중간 지점을 기준으로 삼는다. 하드코딩돼 있던 「00420 · 도시철도 1·2호선 환승」도 데이터에서 나오는 값으로 교체. 도시 정책 문구의 반월당(실제 지명)은 그대로 두되, **급행1에 묶여 있던 서술만 실제 경유지로**(반월당~범어 → 아양교~불로).
- 검증: 빌드 통과 / 노선 3개 폴리라인·정류장 핀 렌더 / 칩 토글(급행1 끄면 path 32→19, 복귀 32, 주요 노선 켜면 35) / 줌 13→15에서 핀 35→79 / 차량이 실제 정류장명으로 주행(불로화훼단지·내동·도학교2) / 승객 앱 3개 노선 ETA 정상.
- **중구·달구벌대로·신천대로·앞산 축 보강 (2026-08-20, 사용자 요청)**: 표시 노선 3 → **6개**. 후보 노선의 폴리라인이 각 축의 사각형을 실제로 지나는지 계산해 골랐다 — 이름만 보고 고르면 어긋난다.
  - **급행2**(19.6km) 중구 도심·달구벌대로·신천대로 / **순환3-1**(31.2km) 앞산순환로·달구벌대로·신천대로 / **순환3**(13.9km) 신천대로·앞산 — 기존 급행6·순환2-1·급행4와 합쳐 6개.
  - 제외: 급행7·급행8은 세 축 어디도 지나지 않고(급행8은 정류장 0), 356·성서2는 OSM에 도로가 1~2개뿐, 급행5는 4.4km로 잘려 있다.
  - **주요 노선 레이어를 기본 ON으로** — 요청의 핵심이 「대구의 주요 버스들이 보이는 것」이라 첫 화면에서 보여야 한다. 점선·42% 불투명도로 실증 3노선을 가리지 않고, 복잡하면 칩 한 번으로 끈다.
  - 축 판정 방법(재사용): 폴리라인 점이 축 bbox 안에 2개 이상 들어가면 그 축을 지나는 것으로 본다. 중구 35.855–35.878/128.578–128.605 · 달구벌대로 35.849–35.863/128.49–128.63 · 신천대로 35.82–35.90/128.593–128.617 · 앞산 35.812–35.842/128.55–128.62.
- **「달구벌대로를 가로지르는 노선」 (2026-08-20, 사용자 요청)**: 방향 감으로 고르지 않고 **도로 형상과 노선 형상의 선분 교차를 직접 계산**했다(`scripts/check-crossing.py`). 달구벌대로 way 160개(1,355 선분)를 받아 노선별 교차 횟수를 셈 — 급행2 7회 · 급행6 5회 · 순환2 3회 · 순환3-1 2회 · 순환2-1/급행7/순환3 각 1회, 급행1·3·4·5·8·8-1·9·9-1은 **0회**. 이미 올린 6개 중 5개가 교차 노선이었고 빠진 하나가 **급행7**이라 그것만 추가(표시 7개). OSM 매핑 범위에서 교차 노선은 이게 전부다.
- **간선도로 레이어 신설**(`src/sim/roads.ts` + `scripts/emit-roads.py`): 달구벌대로·신천대로·앞산순환로·중앙대로를 OSM 도로 이름으로 수집해 지도에 깐다. **이어 붙이지 않고 조각 그대로 그린다** — 도로는 갈래가 많아 하나로 엮으면 없는 연결이 생긴다. 노선보다 **먼저** 그려야(z-order) 도로가 노선을 덮지 않아 「어디서 가로지르는지」가 보인다. 칩으로 토글, 기본 ON.
- **성능 함정**: 도로 폴리라인 365개를 그냥 렌더하면 엔진이 250ms마다 스냅샷을 갈아끼울 때마다 전부 재조정된다 → **`memo`로 묶은 `RoadLayer`**. 도로는 변하지 않는 배경이므로 리렌더 이유가 없다. (라이브 뷰에 정적 대량 벡터를 얹을 때 재사용할 것.)
- **Bash 힙독 함정**: 이 환경의 `<<'PY'` 힙독은 백슬래시를 보존하지 않는다 — 파이썬 소스에 `'
'`을 넣으면 실제 줄바꿈이 되어 문자열이 깨진다. 줄바꿈이 필요하면 **`chr(10)`**을 쓸 것.
- 검증: 두 저장소 빌드 통과 / `#ask` 새로고침 후 AI Q 유지 / 교차검증·한계 버튼 렌더 → 로드맵 이동 → 복귀 시 대화 보존 확인 / 보고서 Blob 2,392바이트 실캡처(절·출처·한계 포함) / 조사 «출입고가»로 교정 확인.
